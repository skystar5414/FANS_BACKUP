# Node.js 20 Alpine 이미지 사용 (개발용)
FROM node:20-alpine

# 작업 디렉토리 설정
WORKDIR /app

# 패키지 파일 복사
COPY package*.json ./

# 모든 의존성 설치 (dev dependencies 포함)
RUN npm ci

# 소스 코드 복사
COPY . .

# bcrypt 등 네이티브 모듈 재설치
RUN npm rebuild

# 포트 설정
EXPOSE 3000

# 개발 환경 설정
ENV NODE_ENV=development

# ts-node-dev로 핫 리로딩 실행
CMD ["npm", "run", "dev"]